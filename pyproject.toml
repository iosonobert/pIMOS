[build-system]
requires = ["setuptools>=80.9.0", "wheel", "setuptools_scm[toml]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "pIMOS"
dynamic = ["version"]
dependencies = [
    "numpy>=1.24.2",
    "scipy",
    "matplotlib",
    "xarray",
    "pandas",
]

[project.optional-dependencies]
read = [
    "dolfyn==1.2.0",
    "seabird",
    "afloat",
]
process = [
    "dolfyn==1.2.0",
    "turbo_lance",
    "afloat",
    "d2spike"
]

[tool.setuptools.packages.find]
include = ["pIMOS", "pIMOS.*"]
exclude = ["build", "dist", "notebooks", "make_gif", "postgres", "reporting", "source_py_code"]

[tool.setuptools_scm]
write_to = "pIMOS/_version.py"
fallback_version = "1.1.2"
local_scheme = "no-local-version"

[tool.pytest.ini_options]
markers = [
    "install: integration smoke test that creates a clean virtual environment and installs pIMOS",
    "runtime: integration test that runs runtime smoke checks inside a fresh virtual environment",
]